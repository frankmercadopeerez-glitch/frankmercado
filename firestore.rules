rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Esta aplicación es privada. Ningún dato financiero debe ser público.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
